services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: autobidd
      POSTGRES_USER: autobidd
      POSTGRES_PASSWORD: autobidd
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
